# RabbitMQ TLS configuration (development)
# Encrypted AMQP only — the plaintext listener is disabled so nothing can
# connect without TLS. The server uses a self-signed certificate; dev clients
# connect with AMQP_SSLMODE=require (skip-verify), so no CA is needed.

# Disable the plaintext AMQP listener (5672); accept TLS only.
listeners.tcp = none
listeners.ssl.default = 5671

ssl_options.certfile = /etc/rabbitmq/certs/server.crt
ssl_options.keyfile  = /etc/rabbitmq/certs/server.key

ssl_options.verify = verify_none
ssl_options.fail_if_no_peer_cert = false
