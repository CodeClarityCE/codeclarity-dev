CC_BASE_URL=https://localhost/api
# devcontainer / agent shell (no docker): http://172.17.0.1:3000
CC_EMAIL=john.doe@codeclarity.io
CC_PASSWORD=ThisIs4Str0ngP4ssW0rd?

# REQUIRED: GitHub classic PAT, scope `public_repo` (or `repo`). Used to
# create the CodeClarity GitHub integration (so projects import as git repos,
# not FILE uploads) and to lift the GitHub API rate limit from 60/h to
# 5000/h. Put it in .env (gitignored) and nowhere else.
GITHUB_TOKEN=
